{% extends 'base.html.twig' %}

{% block title %}Bookshelf{% endblock %}

{% block body %}
    <h1>Bookshelf</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bookshelf.id }}</td>
            </tr>
            <tr>
                <th>Shelf</th>
                <td>{{ bookshelf.shelf }}</td>
            </tr>
            <tr>
                <th>Cookbooks</th>
                <td>{% for cookbook in bookshelf.cookbooks %}
                        <li><a href="{{ path('app_cookbook_show', {'id' : cookbook.id}) }}">{{ cookbook }}</a></li>
                    {% endfor %}</td>
            </tr>
            <tr>
                <th>Cookbooks</th>
                <td>{% for cookbook in bookshelf.cookbooks %}
                        <li><a href="{{ path('app_bookshelf_cookbook_show', {'bookshelf_id': bookshelf.id, 'cookbook_id' : cookbook.id}) }}">{{ cookbook }}</a></li>
                    {% endfor %}</td>
            </tr>
        </tbody>
    </table>

   



    <a href="{{ path('app_member_show', {'id' : bookshelf.member.id}) }}"> back to member </a>

    <a href="{{ path('app_bookshelf_edit', {'id': bookshelf.id}) }}">edit</a>

    {{ include('bookshelf/_delete_form.html.twig') }}
{% endblock %}
