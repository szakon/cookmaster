{% extends 'base.html.twig' %}

{% block title %}Kitchen{% endblock %}

{% block body %}
    <h1>Kitchen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kitchen.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kitchen.name }}</td>
            </tr>
            <tr>
                <th>Published</th>
                <td>{{ kitchen.published }}</td>
        </tbody>
    </table>

    <a href="{{ path('app_member_show', { 'id': member.id}) }}">back to member</a>


    <a href="{{ path('app_kitchen_edit', {'id': kitchen.id}) }}">edit</a>

    {{ include('kitchen/_delete_form.html.twig') }}
{% endblock %}
